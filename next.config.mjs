/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
      domains: ['i.scdn.co'], // Add any other domains you might use for images
    },
};

export default nextConfig;
